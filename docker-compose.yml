version: '3.8'

services:
  db:
    image: postgres:latest
    ports:
      - 5432:5432
    env_file:
      - .db.env
  backend:
    image: oxxydde/my-profile-backend
    build: .
    ports:
      - 8080:8080

